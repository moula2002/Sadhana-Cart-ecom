import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    port: 3000
  },
  build: {
    target: 'esnext',
    minify: 'esbuild',
    cssCodeSplit: true,
    chunkSizeWarningLimit: 1000,
    rollupOptions: {
      // Let Vite handle chunking automatically to prevent React dependency splitting errors
    }
  },
  esbuild: {
    drop: ['console', 'debugger']
  }
});
